[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  included_files = ["node_modules/**"]

[dev]
  command = "npm run dev"
  port = 5173
  targetPort = 8888
